services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: tedx-backend:local
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped